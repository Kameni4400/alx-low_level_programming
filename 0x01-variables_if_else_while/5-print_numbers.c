#include <stdio.h>

/**
*	Description: main - print all digit number of base,
*	Return: 0 if success.
*/
int main(void)
{
	int num;

	for (num = 0; num <= 9 ; num++)
		printf("%d", num);
	puts("");
	return (0);
}
